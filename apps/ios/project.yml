name: AIRunningCoach
options:
  deploymentTarget:
    iOS: "18.0"
targets:
  AIRunningCoach:
    type: application
    platform: iOS
    sources:
      - AIRunningCoach
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.example.airunningcoach
        SWIFT_VERSION: 5.10
        INFOPLIST_FILE: AIRunningCoach/Info.plist
        CODE_SIGN_ENTITLEMENTS: AIRunningCoach/AIRunningCoach.entitlements
  AIRunningCoachTests:
    type: bundle.unit-test
    platform: iOS
    sources:
      - AIRunningCoachTests
    dependencies:
      - target: AIRunningCoach
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.example.airunningcoach.tests
        SWIFT_VERSION: 5.10
        GENERATE_INFOPLIST_FILE: YES
  AIRunningCoachUITests:
    type: bundle.ui-testing
    platform: iOS
    sources:
      - AIRunningCoachUITests
    dependencies:
      - target: AIRunningCoach
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.example.airunningcoach.uitests
        SWIFT_VERSION: 5.10
        GENERATE_INFOPLIST_FILE: YES
